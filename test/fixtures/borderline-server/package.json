{
  "name": "git-mcp-borderline",
  "version": "2.1.0",
  "description": "A legit MCP server that uses safe argv exec and a scoped workspace. Looks scary, is fine — false-positive guard.",
  "type": "module",
  "bin": {
    "git-mcp-borderline": "src/index.js"
  },
  "scripts": {
    "build": "tsc -p . && cp -r assets dist/*",
    "postinstall": "node ./scripts/build-native.js",
    "prepare": "husky install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "3.23.8",
    "js-yaml": "4.1.0",
    "lodash": "4.17.21"
  }
}
